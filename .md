# WhatsApp Çeviri Botu

Bu proje, terminal/komut satırı üzerinden çalışan, gelen WhatsApp mesajlarını otomatik olarak Türkçe'ye, giden Türkçe mesajları ise seçili hedef dile çeviren bir WhatsApp Botudur. Ayrıca **Wit.ai** entegrasyonu sayesinde gelen sesli mesajları yazıya döküp çevirebilir.

## 🚀 Özellikler

- **QR Kod İle Giriş:** Terminal üzerinde oluşturulan QR kodu telefonunuzdan okutarak hızlıca giriş yapabilirsiniz.
- **Metin Çevirisi:** Google Translate altyapısı (API Key gerektirmez) ile anlık ve ücretsiz çeviri yapar.
- **Sesli Mesaj Çevirisi:** Gelen sesli mesajları (ses kayıtlarını), Wit.ai kullanarak önce metne döker, ardından Türkçe'ye çevirir.
- **Otomatik Çeviri:** 
  - Karşıdan gelen mesajların dili algılanıp otomatik olarak Türkçe'ye çevrilir ve terminalde gösterilir.
  - Sizin girdiğiniz mesajlar otomatik olarak seçili hedef dile çevrilip karşı tarafa gönderilir.
- **Çoklu Dil Desteği:** İngilizce (`en`), Rusça (`ru`), Almanca (`de`), İtalyanca (`it`), İspanyolca (`es`) ve Türkçe (`tr`).
- **Kişi Yönetimi:** Terminal üzerinden kişi ekleyebilir, listeleyebilir ve mesajlaşılacak kişiyi atayabilirsiniz (Kişiler `contacts.json` dosyasına kaydedilir).
- **Oturum Yönetimi:** Seçili dil ve aktif kişi bilgileri kaydedilir, bot yeniden başlatıldığında kaldığınız kişiden ve dilden devam edebilirsiniz (`session_state.json`).

## 📋 Gereksinimler

Projenin çalışması için sisteminizde aşağıdaki yazılımların kurulu olması gerekmektedir:
- [Node.js](https://nodejs.org/) (v14 veya üzeri önerilir)
- npm (Node Package Manager - Node.js ile birlikte gelir)
- (Windows ortamında pratik başlatmak için `baslat.bat` dosyası kullanılabilir)

## 🛠️ Kurulum

1. Repoyu bilgisayarınıza indirin veya klonlayın:
   ```bash
   git clone https://github.com/kullaniciadi/proje-adi.git
   cd proje-adi
   ```

2. Gerekli kütüphaneleri (bağımlılıkları) kurun:
   ```bash
   npm install
   ```

3. Çevresel değişkenleri (`.env`) ayarlayın:
   Ana dizinde `.env` isimli bir dosya oluşturun (veya varsa düzenleyin) ve `WIT_AI_TOKEN` değişkenini ekleyin. Gelen sesli mesajların metne dönüştürülmesi için bu adım şarttır. (Wit.ai üzerinden uygulaması oluşturarak ücretsiz token alabilirsiniz).
   ```env
   WIT_AI_TOKEN=sizin_wit_ai_server_access_tokeniniz
   ```

## 🚀 Kullanım

Projeyi başlatmak için ana dizindeki **`baslat.bat`** dosyasına çift tıklayabilirsiniz.
Eğer terminal kullanıyorsanız şu komutu çalıştırabilirsiniz:

```bash
npm start
```

Bot ilk kez başladığında ekranda bir WhatsApp QR kodu belirecektir. WhatsApp uygulamanızdan internete bağlı cihazlar (Bağlı Cihazlar > Cihaz Bağla) menüsünü açarak bu QR kodu okutun.

### 💬 Terminal Komutları

Bot sisteme bağlantı sağladığında komut satırında şu işlemleri yapabilirsiniz:

- **1. Ekle:** `add | 90555xxxxxxx | isim` (Yeni bir kişiyi numarası veismiyle yerel rehbere ekler.)
- **2. Listele:** `list` (Rehbere eklediğiniz kayıtlı kişileri numaralarıyla ekranda listeler.)
- **3. Seç:** `select | isim` (VEYA listedeki sıra numarası örn: `1`) (Hızlıca mesaj göndermek istediğiniz aktif muhatabı seçer.)
- **4. Bırak:** `unselect` (Şu anda mesajlaştığınız aktif kişiyi serbest bırakır/iptal eder.)
- **5. Dil:** `lang | [en/ru/de/it/es/tr]` (Karşı tarafa gidecek mesajların hedef çeviri dilini değiştirir.)
- **6. Gönder:** 
  - `isim | mesaj` (Belirli bir rehber kişisine direkt çevrilmiş mesaj yollar.)
  - Sadece `mesaj` (Eğer aktif bir kişi -select ile- seçiliyse doğrudan o kişiye mesajı yollar.)
- **7. Yenile:** `f5` (Terminal ekranını temizler, rehberi diskten tekrar yükler ve aktif muhatap seçimini sıfırlar.)

## 📦 Kullanılan Teknolojiler / Altyapı

- [whatsapp-web.js](https://wwebjs.dev/): Arka planda başsız tarayıcı (puppeteer) ile WhatsApp Web hizmetine bağlanır.
- [google-translate-api-x](https://github.com/AidanWelch/google-translate-api): Ücretsiz ve anahtarsız Google Çeviri arayüzü sağlar.
- **Wit.ai API**: Gelen WhatsApp ses kayıtlarını (`audio/ogg`) çözümleyip yazıya (`Speech-to-Text`) döker.
- [qrcode-terminal](https://www.npmjs.com/package/qrcode-terminal): Oturum açmak için gereken QR kodu komut satırında çizer.

## ⚠️ Önemli Uyarılar

- Bu bot resmi bir WhatsApp API (WhatsApp Business API vb.) **kullanmamaktadır**. Web versiyonunu (`whatsapp-web.js` kütüphanesini) otomatize eder. Dolayısıyla kurumsal olmayan bu altyapıyı kullanmak WhatsApp tarafından hesabınızın kısıtlanmasına (ban riskine) yol açabilir. Lütfen şahsi veya bir test numarası üzerinde temkinli kullanın.
- "google-translate-api-x" modülü sömürüldüğünde (çok kısa sürede aşırı binlerce istek) Google tarafından geçici olarak IP engellemesine maruz kalabilirsiniz.

## � GitHub'a Yüklerken Dikkat Edilmesi Gerekenler

Eğer projeyi kendi GitHub hesabınıza yükleyecekseniz veya geliştirecekseniz, **bazı dosya ve klasörleri KESİNLİKLE GitHub'a yüklememeniz gerekir**.

Bunu otomatik olarak engellemek için projenin bulunduğu klasörde **`.gitignore`** isimli, başında nokta olan bir dosya oluşturup içine aşağıdakileri kopyalamanız çok önemlidir:

```text
node_modules/
npm-debug.log*
.env
.wwebjs_auth/
.wwebjs_cache/
session_state.json
contacts.json
.DS_Store
Thumbs.db
```

**Bu dosyaların/klasörlerin yüklenmesi neden tehlikeli veya gereksizdir?**
- `node_modules/`: Projenin çalışması için `npm install` komutu ile indirilen yüzlerce megabaytlık kod kütüphaneleridir. Kolayca baştan indirilebildiği için taşınması çok fazla boşa yer kaplar.
- `.wwebjs_auth/` ve `.wwebjs_cache/`: Bunlar **WhatsApp Web oturum (session) çerezlerinizi** saklar! Eğer GitHub'a yüklerseniz başka bir geliştirici bu dosyaları indirerek sizin WhatsApp hesabınıza kendi bilgisayarından giriş yapıp sohbetlerinizi okuyabilir. Kritik güvenlik riskidir.
- `.env`: Sizin kendi özel Wit.ai servis token (şifre) bilginizi içerir. Başkalarının hesabınızdaki kotanızı harcamaması için gizli kalmalıdır.
- `contacts.json`: Sizin eklediğiniz rehberdeki kişi numaralarını ve numara eşleştirmelerini saklar. Başkalarıyla kişisel verilerinizi paylaşmamak adına yüklenmemelidir.

## �📄 Lisans

Bu proje [ISC](https://opensource.org/licenses/ISC) lisansı ile dağıtılmaktadır.
